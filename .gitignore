# Local Python artifacts
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.coverage

# Local packaging / virtualenv outputs
.venv/
venv/
build/
dist/
*.egg-info/

# Local editor / OS noise
.DS_Store

# Repo-local fallback runtime state
# Keep runtime state outside Git when local experiments point MICSYNC_HOME here.
data/
