[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "micsync"
version = "0.1.0"
description = "Magnetar host-side importer for DJI Mic recordings"
requires-python = ">=3.11"
